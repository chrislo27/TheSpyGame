apply plugin: "java"

sourceCompatibility = 1.7
[compileJava, compileTestJava]*.options*.encoding = 'UTF-8'

main{
        java.srcDirs [ "src" ]
        java.srcDirs "ionium-core-src"
    }


eclipse.project {
    name = appName + "-core"
}
